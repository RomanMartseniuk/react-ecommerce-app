import { cn } from "@/lib/utils";

export const Search = ({className}: {className?: string}) => {
  return (
    <div className={cn(className, "bg-background")}>
      <input type="text" name="" id="" />
    </div>
  );
}
